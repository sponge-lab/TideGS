# TideGS Project Page

This repository contains the static GitHub Pages version of the TideGS project website.

Open `index.html` directly in a browser, or serve locally:

```bash
python -m http.server 4000
```
